#ifndef CRYPTOR_HPP
#define CRYPTOR_HPP
#include "hashmap.hpp"
#include "tree.hpp"
#include <vector>
class cryptor
{
public:
	//TAKE DATA STRUCTURES BY REFERENCE
	//cryptor(tokens, map); constructor for decryption
	//cryptor(tokens, map, tree); //constructor for encryption
	//void encrypt(cipher_file_name); //updates cipher as novel chars are disovered
	//void decrypt();
	//void append_to_file(std::string file_name)	
private:
	//std::string result
	//std::vector<std::string> tokens
	//hashmap map
	//tree bst
};

#endif //CRYPTOR_HPP
